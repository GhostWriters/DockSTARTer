name: CI

on:
  push:
    branches:
      - master

jobs:

  yamllint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: yamllint
        run: |
          while IFS= read -r line; do echo "Linting ${line}"; docker run --rm -v ${PWD}:/yaml sdesbure/yamllint yamllint -d '{extends: default, rules: {document-start: {present: false}, line-length: disable}}' "${line}" || exit 1; done < <(git ls-files '*.yaml' '*.yml')

  markdownlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: markdownlint
        run: |
          echo '{"default": true,"line_length": false,"no-inline-html": false,"first-line-h1": false}' > .markdownlint.json
          while IFS= read -r line; do echo "Linting ${line}"; docker run --rm -v ${PWD}:/markdown 06kellyjac/markdownlint-cli "${line}" || exit 1; done < <(git ls-files '*.md')

  deploy:
    needs: [yamllint, markdownlint]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: deploy
        uses: JamesIves/github-pages-deploy-action@releases/v2
        env:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          BASE_BRANCH: master
          BRANCH: gh-pages
          BUILD_SCRIPT: docker run --rm -v ${PWD}/site:/site -v ${PWD}:/docs squidfunk/mkdocs-material build --verbose --clean --strict
          CNAME: dockstarter.com
          FOLDER: site
