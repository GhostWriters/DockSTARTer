description: "Grafana - Analytics and monitoring dashboards"
image: "grafana/grafana:latest"
ports:
  - "3000:3000"
volumes:
  - "${DATA_DIR}/grafana:/var/lib/grafana"
environment:
  - GF_SECURITY_ADMIN_PASSWORD=admin
  - TZ=${TZ}
restart: unless-stopped
