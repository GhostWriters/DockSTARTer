services:
  guacamole:
    container_name: guacamole
    environment:
      - EXTENSIONS=${GUACAMOLE_EXTENSIONS}
      - TZ=${TZ}
    labels:
      com.dockstarter.guacamole_description: >-
        Web application that provides access to desktop environments using
        remote desktop protocols
      com.dockstarter.guacamole_nicename: Guacamole
    logging:
      driver: json-file
      options:
        max-file: ${DOCKERLOGGING_MAXFILE}
        max-size: ${DOCKERLOGGING_MAXSIZE}
    restart: unless-stopped
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKERCONFDIR}/guacamole:/config
      - ${DOCKERSHAREDDIR}:/shared
