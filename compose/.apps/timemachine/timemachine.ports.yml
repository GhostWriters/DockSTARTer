services:
  timemachine<__instance>:
    ports:
      - ${TIMEMACHINE<__INSTANCE>__PORT_137?}:137/udp
      - ${TIMEMACHINE<__INSTANCE>__PORT_138?}:138/udp
      - ${TIMEMACHINE<__INSTANCE>__PORT_139?}:139
      - ${TIMEMACHINE<__INSTANCE>__PORT_445?}:445
