services:
  synclounge:
    container_name: ${SYNCLOUNGE_CONTAINER_NAME}
    environment:
      - TZ=${TZ}
    restart: ${SYNCLOUNGE_RESTART}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKER_VOLUME_STORAGE}:/storage
