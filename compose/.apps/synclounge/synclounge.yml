services:
  synclounge:
    container_name: ${SYNCLOUNGE__CONTAINER_NAME}
    env_file: env_files/synclounge.env
    environment:
      - TZ=${TZ}
    restart: ${SYNCLOUNGE__RESTART}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKER_VOLUME_STORAGE}:/storage
