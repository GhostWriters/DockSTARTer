services:
  flame:
    container_name: ${FLAME__CONTAINER_NAME}
    env_file: env_files/flame.env
    restart: ${FLAME__RESTART}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKER_VOLUME_STORAGE}:/storage
      - ${FLAME__VOLUME_DOCKER_SOCKET}:/var/run/docker.sock
