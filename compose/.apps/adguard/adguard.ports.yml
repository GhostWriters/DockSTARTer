services:
  adguard:
    ports:
      - ${ADGUARD_PORT_3000}:3000
      - ${ADGUARD_PORT_443}:443
      - ${ADGUARD_PORT_80}:80
      - ${ADGUARD_PORT_853}:853
      - ${ADGUARD_SERVERIP}:${ADGUARD_PORT_53}:53
      - ${ADGUARD_SERVERIP}:${ADGUARD_PORT_53}:53/udp
      - ${ADGUARD_SERVERIP}:${ADGUARD_PORT_67}:67/udp
      - ${ADGUARD_SERVERIP}:${ADGUARD_PORT_68}:68
      - ${ADGUARD_SERVERIP}:${ADGUARD_PORT_68}:68/udp
