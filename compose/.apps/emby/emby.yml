services:
  emby:
    container_name: emby
    environment:
      - GID=${PGID}
      - GIDLIST=${PGID}
      - TZ=${TZ}
      - UID=${PUID}
    labels:
      com.dockstarter.appinfo.description: "Organize, play, and stream audio and video"
      com.dockstarter.appinfo.nicename: "Emby"
      com.dockstarter.appvars.emby_backup_config: "true"
      com.dockstarter.appvars.emby_enabled: "false"
      com.dockstarter.appvars.emby_network_mode: ""
      com.dockstarter.appvars.emby_port_8096: "8096"
      com.dockstarter.appvars.emby_port_8920: "8920"
    logging:
      driver: json-file
      options:
        max-file: ${DOCKERLOGGING_MAXFILE}
        max-size: ${DOCKERLOGGING_MAXSIZE}
    restart: unless-stopped
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKERCONFDIR}/emby:/config
      - ${DOCKERSHAREDDIR}:/shared
      - ${MEDIADIR_MOVIES}:/data/movies
      - ${MEDIADIR_MOVIES}:/movies
      - ${MEDIADIR_MUSIC}:/data/music
      - ${MEDIADIR_MUSIC}:/music
      - ${MEDIADIR_TV}:/data/tv
      - ${MEDIADIR_TV}:/tv
