services:
  plex<__instance>:
    devices:
      - ${PLEX<__INSTANCE>__DEVICE_DEV1}
      - ${PLEX<__INSTANCE>__DEVICE_DEV2}
      - ${PLEX<__INSTANCE>__DEVICE_DEV3}
      - ${PLEX<__INSTANCE>__DEVICE_DEV4}
