services:
  quasselweb:
    container_name: quasselweb
    environment:
      - PGID=${PGID}
      - PUID=${PUID}
      - QUASSEL_CORE=${QUASSELWEB_QUASSEL_CORE}
      - QUASSEL_PORT=${QUASSELWEB_QUASSEL_PORT}
      - TZ=${TZ}
      - URL_BASE=${QUASSELWEB_URL_BASE}
    labels:
      com.dockstarter.appinfo.description: "Web client for Quassel. A Quassel-Core instance is required"
      com.dockstarter.appinfo.nicename: "QuasselWeb"
      com.dockstarter.appvars.quasselweb_backup_config: "true"
      com.dockstarter.appvars.quasselweb_enabled: "false"
      com.dockstarter.appvars.quasselweb_network_mode: ""
      com.dockstarter.appvars.quasselweb_port_64080: "64080"
      com.dockstarter.appvars.quasselweb_quassel_core: "quasselcore"
      com.dockstarter.appvars.quasselweb_quassel_port: "4242"
      com.dockstarter.appvars.quasselweb_url_base: "/quassel"
    logging:
      driver: json-file
      options:
        max-file: ${DOCKERLOGGING_MAXFILE}
        max-size: ${DOCKERLOGGING_MAXSIZE}
    restart: unless-stopped
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKERCONFDIR}/quasselweb:/config
      - ${DOCKERSHAREDDIR}:/shared
