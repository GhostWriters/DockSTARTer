services:
  bitwarden:
    container_name: ${BITWARDEN_CONTAINER_NAME}
    environment:
      - TZ=${TZ}
    restart: ${BITWARDEN_RESTART}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKER_VOLUME_CONFIG}/bitwarden:/data
      - ${DOCKER_VOLUME_STORAGE}:/storage
