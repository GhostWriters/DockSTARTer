services:
  syncthing:
    container_name: ${SYNCTHING__CONTAINER_NAME}
    env_file: env_files/syncthing.env
    environment:
      - PGID=${PGID}
      - PUID=${PUID}
      - TZ=${TZ}
    restart: ${SYNCTHING__RESTART}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKER_VOLUME_CONFIG}/syncthing:/config
      - ${DOCKER_VOLUME_STORAGE}:/storage
      - ${SYNCTHING__VOLUME_SOURCEDIR}:/source
