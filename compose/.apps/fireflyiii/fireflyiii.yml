services:
  fireflyiii:
    container_name: ${FIREFLYIII__CONTAINER_NAME}
    env_file: env_files/fireflyiii.env
    environment:
      - TZ=${TZ}
    restart: ${FIREFLYIII__RESTART}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKER_VOLUME_CONFIG}/fireflyiii/export:/var/www/html/storage/export
      - ${DOCKER_VOLUME_CONFIG}/fireflyiii/upload:/var/www/html/storage/upload
      - ${DOCKER_VOLUME_STORAGE}:/storage
