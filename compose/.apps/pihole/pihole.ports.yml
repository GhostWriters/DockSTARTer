services:
  <appname>:
    ports:
      - ${<APPNAME>_PORT_443}:443
      - ${<APPNAME>_PORT_80}:80
      - ${<APPNAME>_ENVIRONMENT_SERVERIP}:${<APPNAME>_PORT_53}:53
      - ${<APPNAME>_ENVIRONMENT_SERVERIP}:${<APPNAME>_PORT_53}:53/udp
      - ${<APPNAME>_ENVIRONMENT_SERVERIP}:${<APPNAME>_PORT_67}:67/udp
