services:
  pihole:
    ports:
      - ${PIHOLE__PORT_443}:443
      - ${PIHOLE__PORT_80}:80
      - ${PIHOLE__IP_SERVERIP}:${PIHOLE__PORT_53}:53
      - ${PIHOLE__IP_SERVERIP}:${PIHOLE__PORT_53}:53/udp
      - ${PIHOLE__IP_SERVERIP}:${PIHOLE__PORT_67}:67/udp
