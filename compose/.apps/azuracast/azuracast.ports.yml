services:
  <appname>:
    ports:
      - ${<APPNAME>_ENVIRONMENT_STATION_PORT_RANGE}:${<APPNAME>_ENVIRONMENT_STATION_PORT_RANGE}
      - ${<APPNAME>_PORT_2022}:2022
      - ${<APPNAME>_PORT_443}:443
      - ${<APPNAME>_PORT_80}:80
