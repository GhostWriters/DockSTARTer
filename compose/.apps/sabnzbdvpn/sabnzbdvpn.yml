services:
  sabnzbdvpn<__instance>:
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    container_name: ${SABNZBDVPN<__INSTANCE>__CONTAINER_NAME?}
    env_file: .env.app.sabnzbdvpn
    environment:
      - PGID=${PGID?}
      - PUID=${PUID?}
      - TZ=${TZ?}
    privileged: true
    restart: ${SABNZBDVPN<__INSTANCE>__RESTART?}
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKER_VOLUME_CONFIG?}/sabnzbdvpn<__instance>:/config
