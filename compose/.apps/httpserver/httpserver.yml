services:
  httpserver:
    container_name: ${HTTPSERVER__CONTAINER_NAME}
    env_file: env_files/httpserver.env
    environment:
      - PGID=${PGID}
      - PUID=${PUID}
      - TZ=${TZ}
    restart: ${HTTPSERVER__RESTART}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKER_VOLUME_STORAGE}:/storage
      - ${HTTPSERVER__VOLUME_HTML}:/html
