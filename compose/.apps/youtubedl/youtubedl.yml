services:
  youtubedl<__instance>:
    container_name: ${YOUTUBEDL<__INSTANCE>__CONTAINER_NAME?}
    env_file: .env.app.youtubedl
    environment:
      - PGID=${PGID?}
      - PUID=${PUID?}
      - TZ=${TZ?}
    restart: ${YOUTUBEDL<__INSTANCE>__RESTART?}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${DOCKER_VOLUME_CONFIG?}/youtubedl<__instance>:/youtube-dl
