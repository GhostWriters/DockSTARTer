services:
  jellyfin<__instance>:
    devices:
      - ${JELLYFIN<__INSTANCE>__DEVICE_DEV1}
      - ${JELLYFIN<__INSTANCE>__DEVICE_DEV2}
      - ${JELLYFIN<__INSTANCE>__DEVICE_DEV3}
      - ${JELLYFIN<__INSTANCE>__DEVICE_DEV4}
