services:
  jellyfin<__instance>:
    devices:
      - ${JELLYFIN<__INSTANCE>__DEVICES_DEV1}
      - ${JELLYFIN<__INSTANCE>__DEVICES_DEV2}
      - ${JELLYFIN<__INSTANCE>__DEVICES_DEV3}
      - ${JELLYFIN<__INSTANCE>__DEVICES_DEV4}
